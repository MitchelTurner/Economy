Synthetic fixture aligned with the deterministic mock extractor (`EXTRACTION_PROVIDER=mock`).
Use as a smoke check that the eval harness itself is wired; replace with photographed receipts for real prompt work.

Also includes a tiny placeholder JPEG so the eval harness exercises the image path (mock extractor ignores pixels).
